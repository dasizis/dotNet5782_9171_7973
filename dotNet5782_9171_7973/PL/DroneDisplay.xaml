<UserControl x:Class="PL.DroneDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:PL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
            <Setter Property="Margin" Value="5,5" />
            <Setter Property="Width" Value="200" />
        </Style>
        <Style TargetType="materialDesign:PackIcon">
            <Setter Property="Foreground" Value="Gray" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,5,0" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Model}"> 
                <Hyperlink
                      Style="{StaticResource MaterialDesignHeadline6Hyperlink}">
                </Hyperlink>
            </TextBlock>
            <TextBlock x:Name="ModelTextBlock" Text="{Binding Model}"/>
            <TextBlock x:Name="weightText" Text="{Binding WeightCategory}"/>
            <TextBlock Text="{Binding Battery}"/>
            <TextBlock Text="{Binding State}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ParcelInDeliver.Id}"/>
                <Button
                    Style="{StaticResource MaterialDesignRaisedLightButton}"
                    ToolTip="Resource name: MaterialDesignRaisedLightButton"
                    Content="_View Parcel Details"
                    IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="MapMarkerRadius"/>
                <TextBlock Text="{Binding Location.Longitude}"/>
                <TextBlock Text="{Binding Location.Latitude}"/>
            </StackPanel>
        </StackPanel>  
    </Grid>
</UserControl>
