<UserControl x:Class="PL.Views.CustomerDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Customer.Id}"/>
        <TextBox Text="{Binding Customer.Name}"/>
        <TextBox Text="{Binding Customer.Phone}"/>
        <TextBlock Text="{Binding Customer.Location.Longitude}"/>
        <TextBlock Text="{Binding Customer.Location.Latitude}"/>

        <Button Content="UPDATE"
                Command="{Binding UpdateCommand}"/>
        <Button Content="DELETE"
                Command="{Binding DeleteCommand}"/>
    </StackPanel>
</UserControl>
