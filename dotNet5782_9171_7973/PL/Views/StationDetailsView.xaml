<UserControl x:Class="PL.Views.StationDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PL.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Station.Id}"/>
        <TextBox Text="{Binding Station.Name}"/>
        <TextBox Text="{Binding Station.EmptyChargeSlots}"/>
        <TextBlock Text="{Binding Station.Location.Longitude}"/>
        <TextBlock Text="{Binding Station.Location.Latitude}"/>
        <Button Content="VIEW DRONES IN CHARGE LIST" Command="{Binding OpenDronesListCommand}"/>
        <Button Content="UPDATE DETAILS" Command="{Binding UpdateDetailsCommand}"/>
    </StackPanel>
</UserControl>
